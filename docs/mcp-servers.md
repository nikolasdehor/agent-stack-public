# MCP Servers

MCPs sao publicados apenas por nome e transporte aproximado. Args, env vars, URLs e auth state ficam fora.

| name | category | transport | public_note |
| --- | --- | --- | --- |
| cloudflare-docs | cloud-deploy | remote-http | Name only. Command args, URLs, env vars, and auth state intentionally omitted. |
| code-review-graph | github-repo-ops | stdio | Name only. Command args, URLs, env vars, and auth state intentionally omitted. |
| memory | ai-agent-ops | stdio | Name only. Command args, URLs, env vars, and auth state intentionally omitted. |
| node_repl | general | stdio | Name only. Command args, URLs, env vars, and auth state intentionally omitted. |
| railway | cloud-deploy | stdio | Name only. Command args, URLs, env vars, and auth state intentionally omitted. |
| telegram | general | stdio | Name only. Command args, URLs, env vars, and auth state intentionally omitted. |
